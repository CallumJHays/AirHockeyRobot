#include "iostream"
#include "stdio.h"
#include "point.h"

class Arm
{
public:
	/*
	All measurements done in millimeters.

	Upper arm defined as the arm connected to the base servo
	(just like a human's upper arm is connected to the shoulder)
	*/
	const int UPPER_ARM_LENGTH = 300;
	const int LOWER_ARM_LENGTH = 350;

	Arm(){

	}

};

int main(){


	return 0;
}